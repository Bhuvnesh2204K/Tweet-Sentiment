[phases.setup]
nixPkgs = ["python39", "gcc", "gcc-unwrapped", "libcxx", "libcxxabi"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate && pip install --upgrade pip",
  ". /opt/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "python app.py" 